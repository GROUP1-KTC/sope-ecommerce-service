services:
  sope-ecommerce-backend:
    image: ${DOCKER_IMAGE_SON}:${DOCKER_TAG}
    container_name: ${DOCKER_CONTAINER}
    env_file:
      - .env
    ports:
      - "${SERV_PORT}:${SERV_PORT}"
    networks:
      - sope-ecommerce-backend-network

networks:
  sope-ecommerce-backend-network:
    external: true
