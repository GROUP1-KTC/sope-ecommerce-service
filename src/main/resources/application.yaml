spring:
  application:
    name: sope ecommerce backend

  datasource:
    url: jdbc:postgresql://localhost:5432/sope
    username: postgres
    password: postgres123
    driver-class-name: org.postgresql.Driver

  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true
    database-platform: org.hibernate.dialect.PostgreSQLDialect
    properties:
      hibernate:
        format_sql: true

  servlet:
    multipart:
      enabled: true
      max-file-size: 10MB
      max-request-size: 20MB

logging:
  level:
    org:
      springframework:
        web: DEBUG
        security: DEBUG

server:
  port: 8080

api:
  prefix: /api/v1

cloudinary:
  cloud-name: dybo8zd4y123
  api-key: 111854312673546
  api-secret: FpFQgyAH3YYHd9tvMtoaTVZcdIM
